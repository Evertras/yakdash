layout:
  screens:
    - stack: horizontal
      children:
        - stack: vertical
          children:
            - name: Hostname
              module: command
              config:
                bash: hostname
        - name: Dir
          style:
            align-horizontal: left
            align-vertical: bottom
          module: command
          config:
            bash: ls --color
